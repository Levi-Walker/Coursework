# Animal JSON Browser

I made this assignment to practice loading JSON with Volley. It requests an array of animal records, converts each object into an `Animal`, and lets the user move forward and backward through the results.

## What I learned

- Making network requests with Volley
- Reading JSON arrays and objects
- Mapping JSON fields to a Java class
- Updating an Activity after an asynchronous response